{
  "name": "prep-ldes-data",
  "author": {
    "name": "Marc van Andel",
    "email": "marc.vanandel@kadaster.nl"
  },
  "license": "MIT",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "watch": "yarn run build -w",
    "hello": "yarn run build && node dist/hello.js",
    "conv": "yarn run build && node dist/convertXml2Json.js"
  },
  "devDependencies": {
    "@types/he": "^1.1.2",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@types/node": "^18.0.0",
    "he": "^1.2.0",
    "txml": "^5.1.1"
  }
}
